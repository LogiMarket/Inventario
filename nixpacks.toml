[phases.setup]
# Ensure Node 20 is available
nixPkgs = ["nodejs_20"]

[phases.install]
# Prefer npm and use package-lock if present
cmds = [
  "npm ci || npm install"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "npm start"
